/// <reference types="astro/client" />

// Kennung des Builds, zur Build-Zeit ersetzt (siehe astro.config.mjs).
// Lokal 'dev' — dann prüft UpdateNotice nicht auf neue Fassungen.
declare const __BUILD_ID__: string;

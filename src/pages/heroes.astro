---
import PageLayout from '../layouts/PageLayout.astro';
import { useTranslations } from '../i18n/utils';

const t = useTranslations('de');
---

<PageLayout title={`${t('heroes.title')} - Wild Hoggs`} description="Hero database with tier rankings">
  <div class="page-header">
    <h1>{t('heroes.title')}</h1>
    <p class="subtitle">Alle Helden mit Tier-Rankings</p>
  </div>

  <div class="content-placeholder">
    <p>Helden-Datenbank wird in Phase 5 implementiert...</p>
  </div>
</PageLayout>

<style>
  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  h1 {
    font-size: 2.5rem;
    color: #ffa500;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .content-placeholder {
    background: rgba(255, 255, 255, 0.05);
    border: 2px dashed rgba(255, 165, 0, 0.3);
    border-radius: 8px;
    padding: 3rem;
    text-align: center;
    color: rgba(255, 255, 255, 0.6);
  }
</style>
